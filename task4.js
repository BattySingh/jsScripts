// Take users name and age and display it onto the screen

'use strict';

// take users name
let userName = prompt("Enter your name", "Guest");

// take users age
let userAge = prompt("What is your age?", "Not interested");

// Displaying input taken from the user
alert(`Hello, ${ userName }. And your age is: ${ userAge }`);