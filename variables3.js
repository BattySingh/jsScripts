// We can change the value of the variable in the code

// declaring the variable with the name message
let message;

// Assigning value of "Hello" to the variable
message = "Hello";

// Assigning the value "World" to the same variable, overriding the previous value
message = "World";

// Displaying the value of the variable message
alert(message);