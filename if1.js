// Working with the conditional branching using if statements

'use strict';

// ask user to input the year
let yourYear = Number(prompt("Enter the year", 2000));

if (yourYear == 2015) {
  alert("The year was matched with a hitting success!");
} else {
  alert("Oh love, you hit the wrong year!");
}