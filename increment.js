// Working with increment operator

'use strict';

let counter = 4;
let x;

x = counter++;     // here value is used first and then incremented
alert(x);

let y = ++counter; // here value is first incremented and then used
alert(y);