// Chaining Assignments

'use strict';

let a, b, c;        // here we have created 3 variables who are undefined
a = b = c = 5+5+3;  // here we are using chain assignment to assign values to it

// displaying the variables values
alert(a);
alert(b);
alert(c);